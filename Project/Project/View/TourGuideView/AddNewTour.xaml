<Window x:Class="Project.View.TourGuideView.AddNewTour"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project.View.TourGuideView"
        mc:Ignorable="d"
        Title="AddNewTour" Height="800" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#506dbb"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#506dbb"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="black"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>

        
        
    </Window.Resources>
    <Grid Background="#28365e">

        <DockPanel>

            <Grid DockPanel.Dock="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Button x:Name="Submit" Content="Submit" Grid.Column="1" Margin="0,10,0,5" Width="300" HorizontalAlignment="Center" Command="{Binding SubmitCommand}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button x:Name="Close" Content="Close" Grid.Column="0" Margin="0,10,0,5" Width="300" HorizontalAlignment="Center" Command="{Binding CloseCommand}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
            


            <Grid DockPanel.Dock="Left" Width="400">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>

                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"></RowDefinition>
                        <RowDefinition Height="75"></RowDefinition>
                        <RowDefinition Height="200"></RowDefinition>
                        <RowDefinition Height="30"></RowDefinition>
                        <RowDefinition Height="30"></RowDefinition>
                        <RowDefinition Height="30"></RowDefinition>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />




                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70"></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>


                        <Label  Grid.Column="0">Tour name:</Label>
                        <TextBox x:Name="tourNameTextbox" Grid.Column="1" Height="20" Text="{Binding Path=NameOfTour, UpdateSourceTrigger=PropertyChanged}">
                        </TextBox>



                    </Grid>

                    <GroupBox BorderBrush="White" BorderThickness="0.5" Grid.Row="1" Margin="5,0,0,0">
                        
                        <GroupBox.Header>
                            <Label Content="Location"/>
                        </GroupBox.Header>

                        <Grid>

                            <Grid.RowDefinitions>
                                <RowDefinition></RowDefinition>

                            </Grid.RowDefinitions>





                            <Grid Grid.Row="0">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <WrapPanel Grid.Column="0">
                                    <Label>Country:</Label>

                                    <ComboBox x:Name="countryComboBox" Width="120" Height="25" VerticalContentAlignment="Center"
                                              ItemsSource="{Binding Path=Countries, UpdateSourceTrigger=PropertyChanged}" 
                                              SelectedValue="{Binding Path=SharedViewModel.Country, UpdateSourceTrigger=PropertyChanged}"
                                              IsEnabled="{Binding IsEnabled}">

                                    </ComboBox>

                                </WrapPanel>
                                <WrapPanel Grid.Column="1">
                                    <Label>City:</Label>
                                    <ComboBox x:Name="cityComboBox" Width="130" Height="25" VerticalContentAlignment="Center"
                                              Text="{Binding Path=SharedViewModel.City, UpdateSourceTrigger=PropertyChanged}"
                                              ItemsSource="{Binding Path=Cities, UpdateSourceTrigger=PropertyChanged}"
                                              SelectedValue="{Binding Path=SharedViewModel.City, UpdateSourceTrigger=PropertyChanged}"
                                              IsEnabled="{Binding IsEnabled}"
                                                ></ComboBox>
                                </WrapPanel>
                            </Grid>


                        </Grid>

                    </GroupBox>


                    <GroupBox Grid.Row="2" BorderBrush="White" BorderThickness="0.5" Margin="5,0,0,0">
                        <GroupBox.Header>
                            <Label>Date and time of tour start</Label>
                        </GroupBox.Header>

                        <Grid>

                            <Grid.RowDefinitions>

                                <RowDefinition></RowDefinition>
                                <RowDefinition></RowDefinition>
                                <RowDefinition Height="110"></RowDefinition>

                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="55*"></ColumnDefinition>
                                <ColumnDefinition Width="45*"></ColumnDefinition>

                            </Grid.ColumnDefinitions>

                            <WrapPanel Grid.Row="0" Grid.Column="0">
                                <Label >Date:</Label>
                                <DatePicker x:Name="date" Width="130" Height="25"
                                            Margin="42,0,0,0"
                                            SelectedDate="{Binding Path=StartDate, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" 
                                            DisplayDateStart="{Binding DatePickerStartDate}"
                                            IsEnabled="{Binding IsEnabled}"></DatePicker>
                            </WrapPanel>
                            <WrapPanel Grid.Row="1" Grid.Column="0">
                                <Label Content="Time(hh:mm)"/>
                                <TextBox x:Name="time" ToolTip="hh:mm" Width="130" Height="20" 
                                         Text="{Binding StartTime, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{Binding IsEnabled}"></TextBox>
                            </WrapPanel>

                            <Button x:Name="AddDate" Grid.Row="1" Grid.Column="1" Margin="2,2,2,5" Width="150"
                                    Content="Add date to list"
                                    Command="{Binding AddDateCommand}"/>

                            <ListBox x:Name="dateTimeList" Grid.Row="2" Grid.ColumnSpan="2" ItemsSource="{Binding Dates, UpdateSourceTrigger=PropertyChanged, StringFormat=g, ConverterCulture=fr-FR}">

                            </ListBox>





                        </Grid>


                    </GroupBox>







                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0">Duration (hours):</Label>
                        <TextBox Grid.Column="1" Height="20" Text="{Binding Path=Duration, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                    </Grid>

                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0">Language:</Label>
                        <ComboBox x:Name="languageComboBox" Grid.Column="1" Height="20"
                                  
                                  SelectedItem="{Binding Path=SharedViewModel.Language, UpdateSourceTrigger=PropertyChanged}"
                                  ItemsSource="{Binding Languages}"
                                  SelectedValue="{Binding SharedViewModel.Language, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  IsEnabled="{Binding IsEnabled}">
                        </ComboBox>
                        
                    </Grid>

                    <Grid Grid.Row="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0">Max number of guests:</Label>
                        <TextBox Grid.Column="1" Height="20" 
                                 Text="{Binding Path=SharedViewModel.GuestNumber, UpdateSourceTrigger=PropertyChanged}" 
                                 IsEnabled="{Binding IsEnabled}"></TextBox>
                    </Grid>


                    <Button Grid.Row="6" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,10,10,0" Height="40" Width="200" Command="{Binding CreateSuggestionCommand}">
                        <TextBlock Text="Create tour against popular location or language" Foreground="Black" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                    </Button>
                    <TextBox Grid.Row="7" Text="{Binding SharedViewModel.Country}"/>
                    <TextBox Grid.Row="8" Text="{Binding SharedViewModel.City}"/>
                    <TextBox Grid.Row="9" Text="{Binding SharedViewModel.Language}"/>


                </Grid>
            </Grid>



            <!--DESNA STRANA-->
            <Grid DockPanel.Dock="Right" Width="350">
                <Grid.RowDefinitions>
                    <RowDefinition Height="230"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="105"></RowDefinition>
                    <RowDefinition Height="20"></RowDefinition>
                    <RowDefinition Height="30"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>

                </Grid.RowDefinitions>


                <GroupBox Grid.Row="0" BorderBrush="white" BorderThickness="0.5">
                    <GroupBox.Header>
                        <Label>Tour points</Label>
                    </GroupBox.Header>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                            <RowDefinition Height="70"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.25*"></ColumnDefinition>
                            <ColumnDefinition Width="0.75*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>


                        <Label Grid.Column="0" Grid.Row="0">Start point:</Label>
                        <TextBox x:Name="startPointTextBox" Grid.Column="1" Grid.Row="0" Height="20" Text="{Binding Path=StartPoint, UpdateSourceTrigger=PropertyChanged}" ></TextBox>



                        <Label Grid.Column="0" Grid.Row="1">End point:</Label>
                        <TextBox x:Name="endPointTextBox" Grid.Column="1" Grid.Row="1" Height="20" Text="{Binding Path=EndPoint, UpdateSourceTrigger=PropertyChanged}"></TextBox>


                        <TextBlock TextWrapping="Wrap" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2">Add more points in the order in which they will be visited (optional):</TextBlock>
                        <Grid Grid.Row="3" Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.65*"/>
                                <ColumnDefinition Width="0.35*"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="pointInput" Grid.Column="0" Height="20" Text="{Binding Path=AnotherPoint, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                            <Button x:Name="AddPoints" Grid.Column="1" Grid.Row="3" Content="Add tour point" Height="20" Margin="5,0,0,0" Command="{Binding AddPointCommand}"/>
                        </Grid>
                        

                        <ListBox x:Name="pointsList" Grid.Row="4" Grid.ColumnSpan="2" ItemsSource="{Binding AnotherPoints, UpdateSourceTrigger=PropertyChanged}"></ListBox>

                    </Grid>
                </GroupBox>




                <Label Grid.Row="1">Description:</Label>
                <TextBox Grid.Row="2" Width="330" Height="100" TextWrapping="Wrap" Text="{Binding Path=Description, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                <Button x:Name="AddPictureButton" Grid.Row="4" Margin="10,0,10,5" Command="{Binding AddPictureCommand}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.5*"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0" Source="../../Resources/Images/image.png" Margin="0,0,15,0"/>
                        <TextBlock Grid.Column="1" Text="Add pictures..." Foreground="Black" TextWrapping="Wrap" VerticalAlignment="Center"/>
                    </Grid>
                </Button>
                
                <ListBox x:Name="imageList" Grid.Row="5" Margin="5,0,5,0" ItemsSource="{Binding Images, UpdateSourceTrigger=PropertyChanged}">


                </ListBox>


            </Grid>
        </DockPanel>

    </Grid>
</Window>
