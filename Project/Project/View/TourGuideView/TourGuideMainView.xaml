<Window x:Class="Project.View.TourGuideView.TourGuideMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project.View.TourGuideView"
        mc:Ignorable="d"
        Title="TourGuideMainView" Height="600" Width="800" Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen">
    <Grid Background="LightBlue">

        <DockPanel>

            <DockPanel>

                <Grid DockPanel.Dock="Left" Width="150" Background="#3d405b">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"></RowDefinition>
                        <RowDefinition Height="150"></RowDefinition>
                        <RowDefinition Height="40"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>


                    <Rectangle Grid.Row="0" Fill="DarkCyan"/>
                    <Label Grid.Row="0" FontSize="25"  Margin="0,0,0,10" Foreground="White"  FontFamily="Century Gothic" HorizontalAlignment="Center">GUIDE</Label>
                    <Image Grid.Row="1" Source="../../Resources/Images/avatar.png" Margin="5"/>
                    <Label x:Name="usernameLabel" Content="Luka" Grid.Row="2" HorizontalAlignment="Center" FontSize="16" FontFamily="Verdana" Foreground="White"></Label>

                    <Grid Grid.Row="3">
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>


                        <Button x:Name="ReviewsBtn" Grid.Row="1"  Margin="5,0,5,10" Click="ReviewsBtn_Click" Background="#e8e4c9" FontFamily="Verdana" Foreground="#212020" FontWeight="Bold">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.5*"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="../../Resources/Images/rating.png" Margin="0,5,10,5"/>
                                <TextBlock Grid.Column="1" Text="Reviews" VerticalAlignment="Center"/>
                            </Grid>
                        </Button>

                        <Button x:Name="StatisticBtn" Grid.Row="2" Margin="5,0,5,10" Click="StatisticBtn_Click" Background="#e8e4c9" FontFamily="Verdana" Foreground="#212020" FontWeight="Bold">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.5*"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="../../Resources/Images/trend.png" Margin="0,5,10,5"/>
                                <TextBlock Grid.Column="1" Text="Statistic" VerticalAlignment="Center"/>
                            </Grid>
                        </Button>

                        <Button x:Name="tourrequestBtn" Grid.Row="3" Margin="5,0,5,10" Background="#e8e4c9" FontFamily="Verdana" Foreground="#212020" FontWeight="Bold" Click="tourrequestBtn_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.5*"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="../../Resources/Images/ask.png" Margin="0,5,10,5"/>
                                <TextBlock Grid.Column="1" Text="Tour requests" VerticalAlignment="Center" TextWrapping="Wrap"/>
                            </Grid>
                        </Button>

                        <Button Grid.Row="4" Margin="5,0,5,10" Background="#e8e4c9" FontFamily="Verdana" Foreground="#212020" FontWeight="Bold">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.5*"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="../../Resources/Images/pdf.png" Margin="0,5,10,5"/>
                                <TextBlock Grid.Column="1" Text="Generate a PDF report"  TextWrapping="Wrap" VerticalAlignment="Center"/>
                            </Grid>

                        </Button>


                    </Grid>



                </Grid>

                <Grid DockPanel.Dock="Right">

                    <Grid.RowDefinitions>
                        <RowDefinition></RowDefinition>
                        <RowDefinition Height="40"></RowDefinition>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="myTourDataGrid" Grid.Row="0" Margin="10" ItemsSource="{Binding Tours, UpdateSourceTrigger=PropertyChanged}"
                                  SelectedItem="{Binding SelectedTour, UpdateSourceTrigger=PropertyChanged}"
                                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                                      SelectionChanged="myTourDataGrid_SelectionChanged"
                                      Background="#F4F1De"
                              GridLinesVisibility="None">
                        <DataGrid.RowStyle>
                            <Style TargetType="{x:Type DataGridRow}">
                                <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                                <Setter Property="IsEnabled" Value="{Binding TourAppointment.IsNotCanceled}"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.RowHeaderStyle>
                            <Style TargetType="{x:Type DataGridRowHeader}">
                                <Setter Property="Height" Value="25"/>
                            </Style>
                        </DataGrid.RowHeaderStyle>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="Height" Value="40"/>
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock TextWrapping="Wrap" Text="{Binding}"></TextBlock>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="3*"></DataGridTextColumn>
                            <DataGridTextColumn Header="Country" Binding="{Binding Location.Country}" Width="1.5*"></DataGridTextColumn>

                            <DataGridTextColumn Header="City" Binding="{Binding Location.City}" Width="1.5*"></DataGridTextColumn>

                            <DataGridTextColumn Header="Language" Binding="{Binding Language}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Max guests number" Binding="{Binding MaxGuests}" Width="1.1*"></DataGridTextColumn>


                            <DataGridTextColumn Header="Duration" Binding="{Binding Duration}"></DataGridTextColumn>
                            <DataGridTextColumn Header="Appointment" Binding="{Binding TourAppointment.DateAndTimeOfAppointment, StringFormat=g, ConverterCulture=fr-FR}" Width="2*"></DataGridTextColumn>

                        </DataGrid.Columns>

                    </DataGrid>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="addTourButton" Grid.Column="0" Margin="10" Click="addTourButton_Click" FontFamily="Verdana">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                            <WrapPanel>
                                <Image Grid.Column="0" Source="../../Resources/Images/add.png" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="1" Text="Add new tour"/>
                            </WrapPanel>
                        </Button>

                        <Button x:Name="cancelTour" Grid.Column="1"  Margin="10" Click="cancelTour_Click" FontFamily="Verdana">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>

                                </Style>
                            </Button.Resources>
                            <WrapPanel>
                                <Image Grid.Column="0" Source="../../Resources/Images/delete.png" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="1" Text="Cancel tour"/>
                            </WrapPanel>



                        </Button>
                    </Grid>

                </Grid>





            </DockPanel>




            
        </DockPanel>
    </Grid>
</Window>
